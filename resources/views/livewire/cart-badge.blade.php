<div>
    @if($count > 0)
        <span class="absolute -top-2 -right-2 bg-[#064e3b] text-white text-[9px] font-bold w-4 h-4 rounded-full flex items-center justify-center shadow-md shadow-black/20">
            {{ $count }}
        </span>
    @endif
</div>
